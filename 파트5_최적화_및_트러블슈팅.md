# 파트 5: 최적화 및 트러블슈팅

## 📋 개요

이 파트는 프로젝트 개발 과정에서 발생한 **주요 문제들과 해결 과정**을 상세히 다룹니다. 각 문제에 대해 **문제 발견 → 원인 분석 → 해결 방법 → 결과** 순서로 설명합니다.

**핵심 목표**: 실제 문제 해결 경험을 통해 기술적 깊이와 문제 해결 능력을 보여줍니다.

---

## 🔍 문제 1: GPU 메모리 부족 (OOM)

### 1.1 문제 발견

**증상**:
- RTX 3060 Ti 8GB 환경에서 배치 크기 16 사용 시 OOM 에러 발생
- 에러 메시지: `RuntimeError: CUDA out of memory`

**영향**:
- 캡션 생성 중단
- 배치 크기를 줄여야 함 (성능 저하)

### 1.2 원인 분석

**메모리 사용량 분석**:

1. **모델 메모리**
   - BLIP 모델 (Float32): 약 4GB
   - BLIP 모델 (Float16): 약 2GB

2. **배치 메모리**
   - 배치 크기 16 (Float32): 약 3GB
   - 배치 크기 16 (Float16): 약 1.5GB

3. **총 메모리**
   - Float32 + 배치 16: 4GB + 3GB = 7GB+ (OOM 발생)
   - Float16 + 배치 16: 2GB + 1.5GB = 3.5GB (안정적)

**근본 원인**:
- Float32 사용으로 모델 메모리 4GB 필요
- 고정 배치 크기로 메모리 여유 무시
- GPU 메모리 최적화 미적용

### 1.3 해결 방법

#### 1단계: FP16 도입

**구현**:
```python
if device == "cuda":
    model.to(device)
    if use_fp16:
        model.half()  # FP16으로 변환
        print(f"   FP16 모드 활성화 (메모리 절약)")
```

**효과**:
- 모델 메모리: 4GB → 2GB (50% 감소)
- 배치 메모리: 3GB → 1.5GB (50% 감소)
- 총 메모리: 7GB+ → 3.5GB (50% 감소)

**품질 영향**:
- FP16은 FP32 대비 거의 동일한 성능 (BLIP 모델에서 검증됨)
- 캡션 품질 저하 없음

#### 2단계: 동적 배치 크기 조정

**구현**:
```python
def get_dynamic_batch_size(device: str, use_fp16: bool) -> int:
    if device != "cuda":
        return 4  # CPU는 작은 배치
    
    # GPU 메모리 확인
    free_memory, total_memory = torch.cuda.mem_get_info(0)
    free_memory_gb = free_memory / (1024**3)
    
    if use_fp16:
        if free_memory_gb > 6:
            return 16
        elif free_memory_gb > 4:
            return 8
        elif free_memory_gb > 2:
            return 4
        else:
            return 2
    else:
        # FP32는 더 작은 배치
        if free_memory_gb > 6:
            return 8
        elif free_memory_gb > 4:
            return 4
        else:
            return 2
```

**로직**:
- 사용 가능 메모리에 따라 배치 크기 자동 선택
- FP16: 2/4/8/16
- FP32: 2/4/8

**효과**:
- 메모리 여유에 따라 최대 성능 활용
- OOM 에러 방지

#### 3단계: CPU 폴백

**구현**:
```python
try:
    # GPU 배치 처리
    captions = generate_captions_batch_gpu(...)
except RuntimeError as e:
    if "out of memory" in str(e):
        print("⚠️  GPU 메모리 부족, CPU로 전환")
        torch.cuda.empty_cache()
        captions = generate_captions_batch_cpu(...)
    else:
        raise
```

**효과**:
- GPU 메모리 부족 시 자동 CPU 전환
- 프로세스 중단 방지
- 안정성 확보

### 1.4 결과

**성능 개선**:
- 메모리 사용량: 7GB+ → 3.5GB (50% 감소)
- 처리 속도: 1.8배 향상 (FP16 + 큰 배치 크기)
- OOM 에러: 0건 (이전: 빈번)

**측정 결과**:
- 배치 크기 16 (FP16): 안정적 동작
- 배치 크기 8 (FP16): 여유 있음
- 배치 크기 4 (FP16): 매우 안정적

---

## 🔍 문제 2: 캡션 반복 문제

### 2.1 문제 발견

**증상**:
- 생성된 캡션에 반복 패턴 다수 발견
- 예: "road road road"
- 예: "country side of the country side"
- 예: "flooded area flooded area"

**영향**:
- 캡션 품질 저하
- 검색 정확도 저하 (중복 패턴이 벡터 공간 왜곡)
- 사용자 경험 저하

**통계**:
- 반복 비율: 약 30%
- 연속 단어 반복: 약 15%
- 구문 반복: 약 15%

### 2.2 원인 분석

**생성 파라미터 문제**:
1. **repetition_penalty 부재**
   - 초기에는 `repetition_penalty` 파라미터 미사용
   - 모델이 이미 생성한 토큰을 재선택

2. **max_length 짧음**
   - 초기 `max_length=50`으로 짧음
   - 정보 압축 과정에서 반복 발생

3. **그리디 디코딩**
   - `do_sample=False`로 그리디 디코딩
   - 국소 최적해에 빠짐

**모델 특성**:
- BLIP 모델이 위성 이미지 도메인에서 반복 경향
- 특히 긴 캡션 생성 시 반복 증가

### 2.3 해결 방법

#### 1단계: 생성 파라미터 최적화

**구현**:
```python
generate_kwargs = {
    "max_length": 120,           # 50 → 120 증가
    "min_length": 20,
    "num_beams": 5,
    "do_sample": True,           # 샘플링 활성화
    "temperature": 0.8,          # 다양성 제어
    "repetition_penalty": 1.3,   # 반복 방지 (1.0보다 크면 반복 감소)
    "length_penalty": 1.2,       # 긴 시퀀스 선호
}
```

**각 파라미터의 역할**:
- `repetition_penalty=1.3`: 이미 생성한 토큰의 확률을 1.3배 낮춤
- `temperature=0.8`: 샘플링 다양성 (너무 높으면 무작위, 너무 낮으면 그리디)
- `do_sample=True`: 샘플링 활성화로 다양성 증가
- `max_length=120`: 더 긴 캡션 허용 (정보량 증가)

**효과**:
- 반복 비율: 30% → 15% (50% 감소)
- 하지만 여전히 반복 발생

#### 2단계: 후처리 스크립트 개발

**알고리즘 설계**:

1. **연속 단어 반복 제거**
   ```python
   # "road road road" → "road"
   words = text.split()
   fixed_words = []
   i = 0
   while i < len(words):
       word = words[i]
       count = 1
       while i + count < len(words) and words[i + count].lower() == word.lower():
           count += 1
       if count >= 2:
           fixed_words.append(word)  # 1개만 유지
           i += count
       else:
           fixed_words.append(word)
           i += 1
   ```

2. **구문 반복 제거**
   ```python
   # "country side of the country side" → "country side of the"
   for length in range(2, min(7, (len(fixed_words) - i) // 2 + 1)):
       segment1 = [w.lower() for w in fixed_words[i:i+length]]
       segment2 = [w.lower() for w in fixed_words[i+length:i+length*2]]
       if segment1 == segment2:
           # 첫 번째만 유지, 나머지 제거
           result_words.extend(fixed_words[i:i+length])
           # 추가 반복도 확인하여 모두 제거
           j = i + length * 2
           while j + length <= len(fixed_words):
               segment_next = [w.lower() for w in fixed_words[j:j+length]]
               if segment_next == segment1:
                   j += length  # 반복 구문 스킵
               else:
                   break
           i = j
   ```

3. **반복적 처리**
   - 최대 10회 반복하여 모든 반복 제거
   - 각 반복마다 새로운 반복 패턴 감지

**구현 파일**: `scripts/data_collection/fix_caption_repetition.py`

**처리 통계**:
- 반복 감지율: 약 30%
- 수정률: 약 25%
- 반복 비율: 30% → 5% 미만 (후처리 후)

### 2.4 결과

**품질 개선**:
- 반복 비율: 30% → 5% 미만 (83% 감소)
- 캡션 가독성 향상
- 검색 정확도 향상 (벡터 공간 왜곡 제거)

**처리 예시**:
- **이전**: "road road road in the middle of the country side of the country side"
- **이후**: "road in the middle of the country side"

---

## 🔍 문제 3: LLM 응답 개수 불일치

### 3.1 문제 발견

**증상**:
- LLM이 "I found 5 images"라고 말하지만 실제로는 24개 이미지가 표시됨
- 사용자 혼란 야기

**원인**:
- 에이전트 내부에서 `top_k=5`로 검색
- 서버에서는 `top_k=24`로 검색
- 검색 결과가 달라서 개수 불일치

### 3.2 원인 분석

**워크플로우 문제**:
1. 서버(`server.py`)에서 `rag_tool.search(query, top_k=24)` 호출
2. 에이전트(`satellite_agent.py`)에서 `rag_tool.search(query, top_k=5)` 다시 호출
3. LLM은 5개 결과만 보고 "5개"라고 답변
4. UI에는 24개 표시

**근본 원인**:
- 검색이 두 번 수행됨
- 에이전트가 서버의 검색 결과를 모름

### 3.3 해결 방법

#### 1단계: 검색 결과 직접 전달

**에이전트 `query` 메서드 수정**:
```python
def query(self, question: str, search_results: List[Dict] = None) -> dict:
    if search_results is not None:
        # 검색 결과가 제공되면 사용, 재검색하지 않음
        initial_state = {
            "messages": [HumanMessage(content=question)],
            "query": question,
            "search_results": search_results,  # 서버에서 검색한 결과 사용
            "final_answer": ""
        }
        # 검색 노드 건너뛰고 답변 생성만 수행
        final_state = self._generate_answer_node(initial_state)
    else:
        # 검색 결과가 없으면 내부에서 검색
        initial_state = {
            "messages": [HumanMessage(content=question)],
            "query": question,
            "search_results": [],
            "final_answer": ""
        }
        final_state = self.graph.invoke(initial_state)
```

**서버 수정**:
```python
# 서버에서 이미 검색한 결과를 에이전트에 전달
resp = agent_instance.query(req.message, search_results=search["results"])
```

**효과**:
- 검색이 한 번만 수행됨
- 서버와 에이전트가 동일한 결과 사용

#### 2단계: 컨텍스트에 총 개수 명시

**컨텍스트 구성 개선**:
```python
total_images = len(search_results)
context_parts = [f"User question: {query}\n\n"]
context_parts.append(f"Total retrieved images: {total_images}\n\n")  # 명시적 개수
```

**프롬프트 개선**:
```python
system_prompt = """...
IMPORTANT: 
- Always use the "Total retrieved images" number when stating how many images were found
- Use the EXACT total number of retrieved images provided in the context
..."""
```

**효과**:
- LLM이 정확한 개수 사용
- 개수 불일치 해결

### 3.4 결과

**개선 전**:
- LLM: "I found 5 images"
- 실제: 24개 표시
- 불일치율: 100%

**개선 후**:
- LLM: "I found 24 images"
- 실제: 24개 표시
- 불일치율: 0%

---

## 🔍 문제 4: UI 레이아웃 스크롤 문제

### 4.1 문제 발견

**증상**:
- 전체 UI가 화면을 벗어남
- 메인 페이지에서 스크롤바 나타남
- 검색 결과가 많을 때 레이아웃 깨짐

**요구사항**:
- 전체 UI가 화면 100%로 나타나야 함
- 스크롤은 검색 결과 그리드와 채팅창 안에만 있어야 함
- 메인 페이지 스크롤 없음

### 4.2 원인 분석

**CSS 문제**:
1. `html, body` 높이 미설정
2. `#app` 높이 미설정
3. Flexbox에서 `min-height: 0` 미설정 (스크롤 비활성화)

**레이아웃 구조**:
- 페이지 전체가 콘텐츠에 맞춰 늘어남
- 내부 패널이 고정 높이가 아님

### 4.3 해결 방법

#### 1단계: 뷰포트 높이 고정

```css
html, body {
  height: 100%;
}

body {
  display: flex;
  flex-direction: column;
  margin: 0;
  padding: 0;
}
```

**효과**: 페이지 높이를 뷰포트에 고정

#### 2단계: 앱 높이 제한

```css
#app {
  height: 80%;  # 뷰포트의 80%
  display: flex;
  flex-direction: column;
  padding: 20px;
  box-sizing: border-box;
}
```

**효과**: 앱이 화면을 벗어나지 않음

#### 3단계: 내부 스크롤 활성화

```css
#layout {
  display: flex;
  gap: 20px;
  flex: 1;
  min-height: 0;  # Flexbox에서 스크롤 활성화
}

#grid {
  flex: 1;
  min-height: 0;  # 필수!
  overflow-y: auto;  # 세로 스크롤
}

#messages {
  flex: 1;
  min-height: 0;  # 필수!
  overflow-y: auto;  # 세로 스크롤
}
```

**핵심**: `min-height: 0`이 없으면 Flexbox에서 스크롤이 작동하지 않음

### 4.4 결과

**개선 전**:
- 페이지 스크롤: 있음
- 레이아웃: 화면을 벗어남

**개선 후**:
- 페이지 스크롤: 없음
- 레이아웃: 화면 80% 고정
- 내부 스크롤: 그리드와 채팅창만 스크롤

---

## 🔍 문제 5: 히어로 섹션 정렬 문제

### 5.1 문제 발견

**증상**:
- "Satellite Geospatial Intelligence" 타이틀이 가운데 정렬되지 않음
- 버튼도 약간 어긋남

**요구사항**:
- 타이틀과 버튼이 정확히 중앙 정렬
- 수평/수직 모두 중앙

### 5.2 원인 분석

**CSS 문제**:
- `display: flex`만 사용하고 `align-items`, `justify-content` 미설정
- 또는 부모 요소의 정렬 설정 불충분

### 5.3 해결 방법

```css
.hero-content {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;      # 수평 중앙
  justify-content: center;  # 수직 중앙
  text-align: center;       # 텍스트 중앙
  margin: 0 auto;          # 추가 중앙 정렬
}
```

**효과**: 완벽한 중앙 정렬

### 5.4 결과

**개선 전**: 약간 어긋난 정렬
**개선 후**: 완벽한 중앙 정렬

---

## 🎯 파트 5 핵심 성과

### 문제 해결 능력

1. **GPU 메모리 최적화**
   - FP16 도입으로 메모리 50% 감소
   - 동적 배치 크기로 성능 최대화
   - OOM 에러 제로

2. **캡션 품질 개선**
   - 생성 파라미터 최적화
   - 후처리 스크립트 개발
   - 반복 비율 83% 감소

3. **시스템 일관성 확보**
   - 검색 결과 직접 전달
   - LLM 응답 개수 정확도 100%

4. **UI/UX 개선**
   - 레이아웃 스크롤 문제 해결
   - 히어로 섹션 정렬 개선

### 기술적 깊이

- **원인 분석**: 메모리 사용량, 생성 파라미터, 워크플로우 등 다각도 분석
- **해결 방법**: 단계적 접근, 여러 옵션 비교
- **검증**: 측정 및 통계로 결과 확인

---

## 🔍 면접에서 강조할 포인트

### 1. GPU 메모리 최적화

**질문**: "GPU 메모리 부족 문제를 어떻게 해결했나요?"

**답변**:
> "RTX 3060 Ti 8GB 환경에서 OOM 에러가 발생했는데, 메모리 사용량을 분석한 결과 Float32 사용과 고정 배치 크기가 원인이었습니다. FP16을 도입하여 모델 메모리를 50% 감소시켰고, 동적 배치 크기 조정으로 사용 가능 메모리에 따라 최적 배치 크기를 자동 선택하도록 했습니다. 또한 GPU 메모리 부족 시 CPU로 자동 전환하는 폴백을 구현하여 안정성을 확보했습니다. 결과적으로 메모리 사용량을 50% 감소시키고 처리 속도를 1.8배 향상시켰으며, OOM 에러를 완전히 제거했습니다."

### 2. 캡션 반복 문제

**질문**: "캡션 반복 문제를 어떻게 해결했나요?"

**답변**:
> "생성 파라미터 최적화와 후처리 스크립트 두 가지 접근을 사용했습니다. 먼저 `repetition_penalty=1.3`, `temperature=0.8`, `do_sample=True` 등으로 생성 시 반복을 줄였지만, 여전히 15% 정도 반복이 발생했습니다. 따라서 후처리 스크립트를 개발하여 연속 단어 반복과 구문 반복(2-6단어)을 감지하고 제거하는 알고리즘을 구현했습니다. 반복적 처리(최대 10회)를 통해 모든 반복 패턴을 제거했고, 반복 비율을 30%에서 5% 미만으로 줄였습니다."

### 3. LLM 응답 개수 불일치

**질문**: "LLM 응답과 실제 표시 개수가 다른 문제를 어떻게 해결했나요?"

**답변**:
> "서버에서 이미 검색을 수행했는데, 에이전트 내부에서 다시 검색하면서 top_k가 달라져서 개수 불일치가 발생했습니다. 해결 방법은 두 가지였습니다. 첫째, 서버에서 검색한 결과를 에이전트에 직접 전달하여 중복 검색을 방지했습니다. 둘째, 컨텍스트에 'Total retrieved images: N'을 명시하고 프롬프트에서 'EXACT total number'를 사용하도록 강하게 지시했습니다. 이를 통해 개수 정확도를 100% 달성했습니다."

---

## 📚 관련 파일

- `scripts/data_collection/generate_captions.py`: GPU 최적화
- `scripts/data_collection/fix_caption_repetition.py`: 반복 제거
- `scripts/agent/satellite_agent.py`: 검색 결과 직접 전달
- `scripts/api/web/static/style.css`: UI 레이아웃 수정

---

## 🔄 전체 프로젝트 연결

파트 5에서 해결한 문제들은 파트 1-4의 모든 단계에 걸쳐 발생했고, 각각의 해결 과정이 전체 시스템의 품질과 성능을 크게 향상시켰습니다. 이러한 문제 해결 경험은 실제 프로덕션 환경에서 필수적인 능력이며, 면접에서 강조할 수 있는 핵심 포인트입니다.

