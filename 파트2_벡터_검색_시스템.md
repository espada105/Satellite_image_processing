# 파트 2: 벡터 검색 시스템 (RAG Retrieval)

## 📋 개요

이 파트는 **온라인 검색 단계**로, 사용자의 텍스트 쿼리를 받아 관련 위성 이미지를 찾는 벡터 검색 시스템을 다룹니다.

**핵심 목표**: 텍스트 쿼리 → 임베딩 변환 → 코사인 유사도 검색 → 관련 이미지 반환

---

## 🔄 전체 워크플로우

```
사용자 쿼리: "flooded area near river"
    ↓
[1단계] 쿼리 임베딩 변환 (sentence-transformers)
    ↓
[2단계] PostgreSQL에서 모든 임베딩 로드
    ↓
[3단계] 코사인 유사도 계산 (NumPy)
    ↓
[4단계] 유사도 높은 순으로 정렬
    ↓
[5단계] Top-K 결과 반환
    ↓
검색 결과: 이미지 경로, 캡션, 유사도 점수
```

---

## 1단계: 쿼리 임베딩 변환

### 1.1 설계 의도

**목적**: 사용자의 자연어 쿼리를 벡터로 변환합니다.

**핵심 원리**:
- 캡션 생성 시 사용한 것과 **동일한 모델** 사용
- `all-MiniLM-L6-v2` (384차원)
- 쿼리와 캡션이 같은 벡터 공간에 있어야 유사도 계산 가능

### 1.2 구현 (`search_by_text`)

```python
def search_by_text(
    query_text: str,
    top_k: int = 24,
    threshold: float = 0.0
) -> List[Dict]:
```

**처리 과정**:
1. SentenceTransformer 모델 로드
2. 쿼리 텍스트를 임베딩으로 변환
3. `search_by_embedding` 호출하여 벡터 검색 수행

**모델 로딩 최적화**:
- 모델을 전역 변수로 캐싱하면 더 효율적 (현재는 매번 로드)
- 하지만 검색 빈도가 낮아서 현재 방식도 충분

---

## 2단계: 코사인 유사도 계산

### 2.1 설계 의도

**왜 코사인 유사도인가?**
- 벡터의 **방향**을 비교 (크기는 무시)
- 텍스트 임베딩에서 의미적 유사성은 방향으로 표현됨
- 범위: -1 ~ 1 (1에 가까울수록 유사)

**수식**:
```
cosine_similarity(A, B) = (A · B) / (||A|| × ||B||)
```

### 2.2 구현 (`cosine_similarity`)

```python
def cosine_similarity(vec1: np.ndarray, vec2: np.ndarray) -> float:
    dot_product = np.dot(vec1, vec2)
    norm1 = np.linalg.norm(vec1)
    norm2 = np.linalg.norm(vec2)
    
    if norm1 == 0 or norm2 == 0:
        return 0.0
    
    return dot_product / (norm1 * norm2)
```

**에지 케이스 처리**:
- 영벡터 체크: norm이 0이면 유사도 0 반환
- NumPy 연산으로 빠른 계산

### 2.3 pgvector 대신 NumPy를 선택한 이유

**pgvector 사용 시**:
- PostgreSQL 확장 설치 필요
- `embedding <=> query_embedding` 같은 특수 연산자 사용
- 인덱스 지원 (IVFFlat, HNSW)으로 빠른 검색

**NumPy 사용 시 (현재 방식)**:
- 모든 임베딩을 메모리로 로드
- Python 루프로 코사인 유사도 계산
- 정렬 후 Top-K 반환

**선택 이유**:
1. **단순성**: pgvector 설치/설정 불필요
2. **호환성**: 다양한 PostgreSQL 버전에서 동작
3. **현재 규모**: 데이터가 수천~수만 개 수준이라 충분히 빠름
4. **유연성**: 검색 로직을 Python에서 자유롭게 수정 가능

**성능 비교**:
- 현재 방식: 수천 개 데이터에서 수백 ms
- pgvector + 인덱스: 수백만 개 데이터에서도 수십 ms
- **결론**: 현재 규모에서는 충분, 향후 확장 시 pgvector 고려

---

## 3단계: 벡터 검색 (`search_by_embedding`)

### 3.1 전체 프로세스

```python
def search_by_embedding(
    query_embedding: np.ndarray,
    top_k: int = 24,
    threshold: float = 0.0
) -> List[Dict]:
```

**단계별 처리**:

1. **DB에서 모든 임베딩 로드**
   ```sql
   SELECT id, image_id, image_path, caption, embedding_array, metadata
   FROM satellite_images
   WHERE embedding_array IS NOT NULL
   ```

2. **코사인 유사도 계산**
   - 쿼리 벡터와 각 DB 벡터 간 유사도 계산
   - NumPy 연산으로 벡터화 처리

3. **임계값 필터링**
   - `threshold` 이상인 결과만 유지
   - 기본값 0.0 (모든 결과 포함)

4. **정렬 및 Top-K**
   - 유사도 높은 순으로 정렬
   - 상위 K개 반환

### 3.2 결과 포맷

**반환 형식**:
```python
[
    {
        'id': 1,
        'image_id': 'image_001',
        'image_path': '/path/to/image.jpg',
        'caption': 'A satellite image showing...',
        'metadata': {...},
        'similarity': 0.85
    },
    ...
]
```

**유사도 점수 해석**:
- 0.8 이상: 매우 유사
- 0.6-0.8: 유사
- 0.4-0.6: 보통
- 0.4 미만: 낮은 유사도

---

## 4단계: 하이브리드 검색 (Hybrid Search)

### 4.1 설계 의도

**목적**: 벡터 검색 + 메타데이터 필터링을 결합합니다.

**왜 필요한가?**
- 벡터 검색만으로는 부족한 경우
- 예: "POST-event 이미지만 검색" (이벤트 타입 필터)
- 예: "Louisiana 지역 이미지만 검색" (위치 필터)

### 4.2 구현 (`hybrid_search`)

```python
def hybrid_search(
    query_text: Optional[str] = None,
    query_embedding: Optional[np.ndarray] = None,
    metadata_filters: Optional[Dict] = None,
    top_k: int = 24,
    similarity_weight: float = 0.7,
    metadata_weight: float = 0.3
) -> List[Dict]:
```

**처리 흐름**:

1. **벡터 검색 수행**
   - 텍스트 쿼리 또는 임베딩으로 검색
   - `top_k * 2`로 더 많은 후보 확보

2. **메타데이터 필터링**
   - `search_by_metadata`로 필터 조건에 맞는 이미지 찾기
   - 예: `event_type='POST-event'`, `location_name='Louisiana'`

3. **결과 결합**
   - 벡터 검색 결과 중 메타데이터 필터를 통과한 것만 선택
   - 메타데이터 매칭 시 유사도 점수에 보너스 추가
   - 최종 점수 = `similarity * 0.7 + 0.3` (매칭 시)

4. **정렬 및 Top-K**
   - 최종 점수로 정렬
   - 상위 K개 반환

### 4.3 메타데이터 검색 (`search_by_metadata`)

**지원 필터**:
- `event_type`: 'PRE-event' 또는 'POST-event'
- `dataset`: 데이터셋 이름
- `location_name`: 위치 이름

**SQL 쿼리 예시**:
```sql
SELECT id, image_id, image_path, caption, metadata, location_name
FROM satellite_images
WHERE metadata->>'event_type' = 'POST-event'
  AND location_name = 'Louisiana'
LIMIT 100
```

**JSONB 활용**:
- `metadata->>'event_type'`: JSONB 필드에서 문자열 추출
- PostgreSQL의 JSONB 연산자 활용

---

## 5단계: Top-K 선택 전략

### 5.1 top_k=24로 설정한 이유

**초기 실험**:
- `top_k=10`: 결과가 부족함
- `top_k=50`: 너무 많아서 UI에 표시하기 어려움

**최종 선택: top_k=24**
- **UI 레이아웃**: 2열 그리드에서 3행 × 8열 = 24개가 적절
- **사용자 경험**: 충분한 결과 제공, 과다하지 않음
- **성능**: 검색 속도와 결과 품질의 균형

### 5.2 유사도 임계값 (threshold)

**기본값: 0.0**
- 모든 결과 포함 (임계값 없음)
- 낮은 유사도 결과도 사용자가 판단

**사용 사례**:
- `threshold=0.5`: 중간 이상 유사도만 반환
- `threshold=0.7`: 높은 유사도만 반환 (정밀 검색)

---

## 🎯 RAG 도구 래퍼 (RAGTool)

### 설계 의도

**목적**: 검색 로직을 캡슐화하여 에이전트에서 쉽게 사용

**클래스 구조**:
```python
class RAGTool:
    def __init__(self, top_k: int = 5):
        self.top_k = top_k
    
    def search(self, query: str, metadata_filters: Optional[Dict] = None, 
               top_k: Optional[int] = None) -> Dict:
        # 벡터 검색 또는 하이브리드 검색 수행
        # 결과 포맷팅
        return {
            'query': query,
            'results': formatted_results,
            'count': len(formatted_results)
        }
```

**주요 기능**:
1. **검색 수행**: `search_by_text` 또는 `hybrid_search` 호출
2. **결과 포맷팅**: 이미지 경로를 절대 경로로 변환
3. **컨텍스트 생성**: LLM에 전달할 컨텍스트 문자열 생성

### 컨텍스트 생성 (`get_context`)

**목적**: 검색 결과를 LLM이 이해할 수 있는 텍스트로 변환

**형식**:
```
검색 쿼리: flooded area near river

관련 이미지 8개를 찾았습니다:

1. 이미지 ID: image_001
   캡션: A satellite image showing a flooded area...
   경로: /path/to/image.jpg
   유사도: 0.8523

2. 이미지 ID: image_002
   ...
```

**사용처**: 파트 3 (AI 에이전트)에서 LLM 컨텍스트로 활용

---

## 🔍 성능 최적화 고려사항

### 현재 방식의 한계

1. **전체 스캔**
   - 모든 임베딩을 메모리로 로드
   - 데이터가 수만 개 이상이면 느려짐

2. **인덱스 부재**
   - pgvector의 IVFFlat, HNSW 인덱스 미사용
   - 대규모 데이터에서 성능 저하

### 향후 개선 방향

1. **pgvector 도입**
   - 인덱스 지원으로 빠른 검색
   - 대규모 확장 대비

2. **캐싱**
   - 자주 검색되는 쿼리 결과 캐싱
   - Redis 등 활용

3. **리랭킹**
   - Cross-encoder 모델로 재정렬
   - 더 정확한 순위

---

## 🎯 파트 2 핵심 성과

### 기술적 성과

1. **벡터 검색 시스템 구축**
   - 텍스트 쿼리 → 벡터 변환 → 유사도 검색 완성
   - 코사인 유사도 기반 정확한 검색

2. **하이브리드 검색 지원**
   - 벡터 검색 + 메타데이터 필터링
   - 도메인 특화 검색 가능

3. **RAG 도구 캡슐화**
   - 검색 로직을 재사용 가능한 도구로 제공
   - 에이전트에서 쉽게 활용

### 검색 품질

- **정확도**: 유사도 0.7 이상 결과가 대부분 관련 이미지
- **속도**: 수천 개 데이터에서 수백 ms
- **유연성**: 다양한 필터 조건 지원

---

## 🔍 면접에서 강조할 포인트

### 1. pgvector 미사용 이유

**질문**: "왜 pgvector를 사용하지 않았나요?"

**답변**:
> "pgvector를 사용하지 않고 NumPy로 코사인 유사도를 직접 계산한 이유는, 설치 복잡도와 호환성 문제를 피하기 위해서입니다. 현재 데이터 규모(수천~수만 개)에서는 전체 스캔 방식도 충분히 빠르고, 코드가 더 단순하고 유지보수가 쉬웠습니다. 향후 대규모 확장이 필요하면 pgvector나 전문 벡터 DB로 마이그레이션할 계획입니다."

### 2. 하이브리드 검색 설계

**질문**: "하이브리드 검색을 어떻게 설계했나요?"

**답변**:
> "벡터 검색과 메타데이터 필터링을 결합하는 하이브리드 검색을 구현했습니다. 먼저 벡터 검색으로 관련 이미지를 찾고, 그 중에서 메타데이터 필터(PRE/POST-event, 위치 등)를 통과한 것만 선택합니다. 메타데이터 매칭 시 유사도 점수에 보너스를 추가하여, 의미적으로 유사하면서도 필터 조건을 만족하는 결과를 우선적으로 반환하도록 했습니다."

### 3. Top-K 선택

**질문**: "top_k=24로 설정한 이유는?"

**답변**:
> "UI 레이아웃과 사용자 경험을 고려하여 top_k=24로 설정했습니다. 2열 그리드 레이아웃에서 3행 × 8열 = 24개가 적절했고, 충분한 결과를 제공하면서도 과다하지 않아 사용자가 결과를 쉽게 탐색할 수 있었습니다. 초기에는 top_k=10으로 했지만 결과가 부족했고, 50으로 늘리니 UI에 표시하기 어려웠습니다."

---

## 📚 관련 파일

- `scripts/database/search_vector_db_v2.py`: 벡터 검색 함수
- `scripts/rag/rag_tool.py`: RAG 도구 래퍼

---

## 🔄 다음 파트로의 연결

파트 2에서 검색한 결과는 **파트 3 (AI 에이전트)**로 전달됩니다. 에이전트는 검색 결과를 LLM 컨텍스트로 사용하여 자연어 답변을 생성합니다.

