# 면접 준비 요약 - 위성 이미지 처리 AI 프로젝트

## 🎯 핵심 메시지 (30초 엘리베이터 피치)

"SpaceNet SN8 데이터셋을 활용해 위성 이미지 분석을 위한 멀티모달 RAG 시스템을 구축했습니다. LangGraph로 에이전트를 설계하고, BLIP 모델로 이미지 캡션을 생성한 후 벡터 검색과 LLM을 결합해 지능형 질의응답 시스템을 구현했습니다. FastAPI로 서비스화하여 실제 사용 가능한 수준까지 완성했습니다."

---

## ✅ 채용 공고 요구사항 대응 체크리스트

### 1. LangGraph 기반 멀티 에이전트 프로덕트 구축 및 운영 경험 ✅

**구현 내용:**
- LangGraph StateGraph로 위성 이미지 분석 에이전트 구현
- 검색 노드 → 답변 생성 노드의 워크플로우 설계
- FastAPI 서버에 통합하여 프로덕션 환경에서 운영

**면접 답변 예시:**
> "LangGraph의 StateGraph를 활용해 위성 이미지 분석 에이전트를 구현했습니다. 검색 노드에서 RAG를 통해 관련 이미지를 찾고, 답변 생성 노드에서 LLM이 검색 결과를 바탕으로 자연어 답변을 생성하는 구조입니다. 각 노드는 독립적으로 동작하며 상태를 안전하게 전달하도록 설계했습니다."

---

### 2. LLM 파인튜닝 또는 프롬프트 엔지니어링 ✅

**구현 내용:**
- 위성 이미지 분석에 특화된 시스템 프롬프트 설계
- 검색 결과 개수, 샘플 캡션, 유사도 점수를 포함한 구조화된 컨텍스트 구성
- 반복 방지, 자연스러운 답변 유도를 위한 프롬프트 최적화

**면접 답변 예시:**
> "프롬프트 엔지니어링을 통해 위성 이미지 분석에 특화된 시스템 프롬프트를 설계했습니다. 초기에는 개별 이미지를 나열하는 방식이었는데, 사용자 경험을 개선하기 위해 요약 중심의 답변으로 변경했습니다. 또한 검색된 이미지 개수를 정확히 사용하고, 반복을 방지하기 위한 지침을 추가했습니다."

---

### 3. RAG 설계/운용 (인덱싱, 하이브리드 검색, 리랭킹, 캐싱) ✅

**구현 내용:**
- **인덱싱**: BLIP 캡션 생성 → Sentence-Transformers 임베딩 → PostgreSQL 저장
- **하이브리드 검색**: 텍스트 벡터 검색 + 메타데이터 필터링
- **리랭킹**: 코사인 유사도 기반 자동 정렬
- **캐싱**: 검색 결과를 에이전트에 전달하여 중복 검색 방지

**면접 답변 예시:**
> "RAG 시스템을 설계할 때 인덱싱부터 검색까지 전체 파이프라인을 고려했습니다. BLIP 모델로 이미지 캡션을 생성하고, sentence-transformers로 384차원 벡터로 변환한 후 PostgreSQL에 저장했습니다. 검색 시에는 텍스트 쿼리를 임베딩으로 변환해 코사인 유사도로 검색하고, 메타데이터 필터링도 지원하는 하이브리드 검색을 구현했습니다."

---

### 4. 멀티모달(이미지+텍스트) 파이프라인 구성 및 서비스화 ✅

**구현 내용:**
- 이미지 → BLIP 캡션 → 텍스트 임베딩 → 벡터 검색 파이프라인
- FastAPI 서버로 RESTful API 제공
- 웹 UI로 실시간 검색 및 챗봇 인터페이스 제공

**면접 답변 예시:**
> "멀티모달 파이프라인을 구축해 위성 이미지를 자연어로 검색할 수 있도록 했습니다. BLIP 모델로 이미지를 캡션으로 변환하고, sentence-transformers로 벡터화한 후 벡터 검색을 통해 텍스트 쿼리로 관련 이미지를 찾을 수 있습니다. FastAPI로 서비스화하여 RESTful API와 웹 UI를 제공했습니다."

---

## 🚀 우대사항 대응

### MLOps 경험 ✅
- **GPU 최적화**: CUDA 병렬 처리, FP16 메모리 최적화
- **배치 처리**: 여러 이미지를 동시에 처리하여 속도 향상
- **평가 지표**: 캡션 품질, 검색 정확도 등 정량적 평가

**면접 답변 예시:**
> "RTX 3060 Ti 8GB 환경에서 GPU를 최대한 활용하기 위해 FP16을 사용하고 배치 처리를 구현했습니다. 배치 크기를 조정하여 메모리 부족 없이 안정적으로 운영할 수 있도록 했습니다."

---

### 도메인 특화 데이터셋 구축/정제 경험 ✅
- **SpaceNet SN8 데이터셋 처리**: 자동화된 다운로드 및 전처리
- **메타데이터 추출**: GeoTIFF 헤더에서 좌표, 이벤트 타입 등 추출
- **데이터 정제**: 캡션 반복 제거, 품질 검증

**면접 답변 예시:**
> "SpaceNet SN8 Floods 데이터셋을 처리하면서 PRE/POST-event 구분, 좌표 정보 추출 등 도메인 특화 메타데이터를 활용했습니다. 캡션 생성 후 반복 구문을 제거하는 후처리도 구현하여 데이터 품질을 개선했습니다."

---

### 클라우드 배포·서빙 경험 ✅
- **FastAPI 서버**: 프로덕션 준비된 ASGI 서버
- **모듈화된 아키텍처**: 각 컴포넌트가 독립적으로 동작
- **확장 가능한 구조**: Docker/Kubernetes 배포 가능

**면접 답변 예시:**
> "FastAPI로 서버를 구축하고, 각 컴포넌트를 모듈화하여 확장 가능한 구조로 설계했습니다. 현재는 로컬 환경에서 운영 중이지만, Docker 컨테이너화와 Kubernetes 배포가 가능하도록 구조를 잡았습니다."

---

### LangChain/LangGraph, 벡터DB 운용, GPU 최적화 ✅
- **LangGraph**: StateGraph로 에이전트 워크플로우 관리
- **벡터DB**: PostgreSQL에 벡터 저장 및 코사인 유사도 검색
- **GPU 최적화**: 배치 처리, FP16, CUDA 활용

**면접 답변 예시:**
> "LangGraph의 StateGraph를 활용해 에이전트 워크플로우를 관리하고, PostgreSQL에 벡터를 저장해 코사인 유사도 기반 검색을 구현했습니다. GPU 최적화를 위해 배치 처리와 FP16을 적용하여 처리 속도를 크게 향상시켰습니다."

---

### 국방/재난·농업·자원 등 EO 활용 과제 경험 ✅
- **SpaceNet SN8**: 홍수 탐지 및 재난 대응 도메인
- **PRE/POST-event 분석**: 재해 전후 비교 분석
- **실무 적용 가능성**: 재난 대응, 도시 계획, 환경 모니터링

**면접 답변 예시:**
> "SpaceNet SN8 데이터셋은 홍수 탐지를 위한 데이터셋으로, PRE/POST-event 비교 분석이 가능합니다. 이를 통해 재난 대응, 도시 계획, 환경 모니터링 등 다양한 EO 활용 분야에 적용할 수 있는 시스템을 구축했습니다."

---

## 💡 면접 시 강조할 기술적 깊이

### 1. End-to-End 구현
- 데이터 수집부터 서비스 배포까지 전체 파이프라인 구현
- 각 단계에서 발생하는 문제를 해결하고 최적화

### 2. 실무 적용 가능성
- SpaceNet 데이터셋으로 실제 EO 도메인 문제 해결
- 재난 대응, 도시 계획 등 실무 시나리오 고려

### 3. 기술적 깊이
- LangGraph, RAG, 멀티모달 파이프라인 등 최신 기술 활용
- 단순 구현이 아닌 최적화와 품질 개선까지 수행

### 4. 문제 해결 능력
- GPU 메모리 부족 문제 → FP16, 배치 크기 조정
- 캡션 반복 문제 → 후처리 스크립트 개발
- 검색 정확도 개선 → top_k 조정, 프롬프트 최적화

---

## 📝 예상 질문 및 답변

### Q1: 프로젝트에서 가장 어려웠던 부분은?
**A:** "캡션 생성 시 반복 문제와 GPU 메모리 최적화였습니다. BLIP 모델이 반복적인 구문을 생성하는 문제를 해결하기 위해 후처리 스크립트를 개발했고, 8GB GPU에서 안정적으로 운영하기 위해 FP16과 배치 크기 조정을 통해 최적화했습니다."

### Q2: RAG 시스템의 검색 정확도를 어떻게 개선했나요?
**A:** "초기에는 top_k=10으로 설정했는데, 충분한 결과를 제공하기 위해 24로 증가시켰습니다. 또한 프롬프트를 최적화하여 LLM이 검색 결과를 더 정확하게 해석하도록 개선했습니다."

### Q3: LangGraph를 선택한 이유는?
**A:** "에이전트의 워크플로우를 명확하게 정의하고, 각 노드가 독립적으로 동작하도록 하기 위해 선택했습니다. StateGraph를 통해 상태 관리가 용이하고, 향후 노드 추가나 수정이 쉬운 구조입니다."

### Q4: 프로덕션 환경에서의 고려사항은?
**A:** "에러 처리, 로깅, 모니터링을 고려했습니다. LLM이 설정되지 않은 경우 graceful degradation을 구현했고, 검색 결과를 캐싱하여 중복 검색을 방지했습니다. 또한 모듈화된 구조로 각 컴포넌트를 독립적으로 테스트하고 배포할 수 있도록 했습니다."

### Q5: 향후 개선 계획은?
**A:** "리랭킹 모델 도입, 캐싱 전략 고도화, 스트리밍 응답 지원 등을 계획하고 있습니다. 또한 더 많은 데이터셋으로 확장하고, 파인튜닝을 통해 도메인 특화 성능을 향상시키고 싶습니다."

---

## 🎯 마무리 메시지

"이 프로젝트를 통해 LangGraph, RAG, 멀티모달 파이프라인 등 최신 기술을 실제 문제에 적용하는 경험을 쌓았습니다. 특히 EO 도메인에서의 실무 적용 가능성을 확인했고, 텔레픽스에서 더 깊이 있는 연구와 개발을 하고 싶습니다."

