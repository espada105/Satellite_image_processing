# 파트 4: 웹 서비스 구현 (FastAPI + UI)

## 📋 개요

이 파트는 **프로덕션 준비 웹 서비스**로, FastAPI 백엔드와 커스텀 프론트엔드 UI를 구현하여 사용자가 위성 이미지를 검색하고 AI 답변을 받을 수 있는 시스템을 다룹니다.

**핵심 목표**: RESTful API 제공 + 직관적인 웹 UI + 실시간 검색 및 챗봇 인터페이스

---

## 🔄 전체 워크플로우

```
사용자 브라우저
    ↓
[1단계] 히어로 섹션 (풀스크린 배경)
    ↓
[2단계] "Enter Experience" 버튼 클릭
    ↓
[3단계] 메인 UI 표시 (2열 레이아웃)
    ↓
[4단계] 사용자 질문 입력
    ↓
[5단계] FastAPI /chat 엔드포인트 호출
    ↓
[6단계] RAG 검색 + AI 에이전트 답변 생성
    ↓
[7단계] 검색 결과 그리드 + 답변 표시
    ↓
[8단계] 이미지 클릭 → 모달 뷰어
```

---

## 1단계: FastAPI 서버 구조

### 1.1 서버 초기화

```python
app = FastAPI(title="Satellite Geospatial RAG API", version="0.2.0")

# 정적 파일/이미지 마운트
app.mount("/static", StaticFiles(directory=str(static_dir)), name="static")
app.mount("/images", StaticFiles(directory=str(images_dir)), name="images")
```

**핵심 구성**:
1. **정적 파일 마운트**: CSS, JavaScript, 이미지 제공
2. **이미지 디렉토리 마운트**: 처리된 위성 이미지 제공
3. **전역 도구**: RAG 도구, 에이전트 인스턴스

### 1.2 엔드포인트 설계

#### 루트 엔드포인트 (`/`)

**역할**: 웹 UI HTML 제공

**구현**:
- 인라인 HTML (템플릿 파일 없이)
- 히어로 섹션 + 메인 UI 구조
- CSS/JS 링크 포함

**캐시 버스팅**:
- `?v=2` 쿼리 파라미터로 브라우저 캐시 무효화
- CSS/JS 수정 시 버전 번호 증가

#### 채팅 엔드포인트 (`/chat`)

**역할**: 사용자 질문 처리, 검색 + 답변 생성

**요청 형식**:
```python
class ChatRequest(BaseModel):
    message: str
    top_k: int = 24
```

**처리 흐름**:
1. RAG 검색 수행 (`top_k=24`)
2. 이미지 경로를 웹 URL로 변환
3. AI 에이전트로 답변 생성 (검색 결과 직접 전달)
4. JSON 응답 반환

**응답 형식**:
```python
{
    "message": "flooded area near river",
    "answer": "I found 24 images of flooded areas...",
    "results": [
        {
            "image_id": "...",
            "image_url": "/images/...",
            "caption": "...",
            "similarity": 0.85
        },
        ...
    ],
    "count": 24
}
```

#### 기타 엔드포인트

- `/search/text`: 텍스트 검색 (RESTful API)
- `/search/hybrid`: 하이브리드 검색
- `/rag`: RAG 검색
- `/agent/query`: 에이전트 직접 호출
- `/health`: 헬스 체크

---

## 2단계: 히어로 섹션 (Hero Section)

### 2.1 설계 의도

**목적**: 첫 화면에서 시각적 임팩트 제공

**요구사항**:
- `earth.jpg` 배경 이미지 (풀스크린)
- 가운데 정렬된 타이틀과 버튼
- "Enter Experience" 버튼 클릭 시 메인 UI로 전환

### 2.2 HTML 구조

```html
<div id='hero'>
  <div class='hero-overlay'></div>
  <div class='hero-content'>
    <h1>Satellite Geospatial Intelligence</h1>
    <p>Explore satellite imagery, search, and AI insights.</p>
    <button id='enterBtn'>Enter Experience</button>
  </div>
</div>
```

**구성 요소**:
- `hero-overlay`: 어두운 그라데이션 오버레이 (텍스트 가독성)
- `hero-content`: 중앙 정렬된 콘텐츠

### 2.3 CSS 스타일링

```css
#hero {
  position: fixed;
  inset: 0;
  background: url('/static/earth.jpg') center/cover no-repeat;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 3000;
  transition: transform 0.9s ease, opacity 0.9s ease;
}

.hero-content {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  color: #ffffff;
  max-width: 580px;
  width: 100%;
  padding: 16px;
  margin: 0 auto;
  background: transparent;
}
```

**핵심 스타일**:
- `position: fixed; inset: 0;`: 풀스크린 고정
- `background: url(...) center/cover`: 배경 이미지 (중앙, 커버)
- `display: flex; align-items: center; justify-content: center;`: 중앙 정렬
- `transition`: 부드러운 전환 애니메이션

### 2.4 JavaScript 전환 로직

```javascript
if (enterBtn && heroEl && appRoot) {
  enterBtn.addEventListener('click', () => {
    heroEl.classList.add('hero-hidden');
    appRoot.classList.remove('app-hidden');
    appRoot.classList.add('app-visible');
    document.body.classList.remove('hero-active');
    setTimeout(() => {
      heroEl.style.display = 'none';
    }, 900);
  });
}
```

**처리 과정**:
1. 버튼 클릭 시 `hero-hidden` 클래스 추가
2. 메인 UI 표시 (`app-hidden` 제거, `app-visible` 추가)
3. 900ms 후 히어로 섹션 완전히 숨김 (성능 최적화)

**애니메이션**:
- `transform: translateY(-100%)`: 위로 슬라이드
- `opacity: 0`: 페이드 아웃
- `transition: 0.9s ease`: 부드러운 전환

---

## 3단계: 메인 UI 레이아웃

### 3.1 2열 레이아웃 설계

**요구사항**:
- 왼쪽: 검색 결과 이미지 그리드
- 오른쪽: 채팅/답변 영역
- 페이지 전체는 고정 높이 (뷰포트 100%)
- 내부 패널만 스크롤 (페이지 스크롤 없음)

### 3.2 HTML 구조

```html
<div id='app' class='app-hidden'>
  <h1>Satellite Geospatial Chat</h1>
  <div id='layout'>
    <div id='results'>
      <h3>검색 결과</h3>
      <div id='grid'></div>
    </div>
    <div id='chatPane'>
      <div id='chat'>
        <div id='messages' aria-live='polite'></div>
        <div id='inputBox'>
          <input id='userInput' type='text' placeholder='질문을 입력하세요'>
          <button id='sendBtn'>Send</button>
        </div>
      </div>
    </div>
  </div>
  <footer id='footer'>
    <p>Developed by seongin Hong &middot; Data sources: SpaceNet, custom satellite datasets</p>
  </footer>
</div>
```

### 3.3 CSS 레이아웃

```css
html, body {
  height: 100%;
}

body {
  display: flex;
  flex-direction: column;
  margin: 0;
  padding: 0;
}

#app {
  height: 80%;
  display: flex;
  flex-direction: column;
  padding: 20px;
  box-sizing: border-box;
}

#layout {
  display: flex;
  gap: 20px;
  flex: 1;
  min-height: 0;
}

#results {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
}

#grid {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: 16px;
}

#chatPane {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
}

#messages {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
}
```

**핵심 CSS 기법**:
1. **Flexbox 레이아웃**
   - `display: flex`: 2열 구조
   - `flex: 1`: 동일한 너비 분할

2. **내부 스크롤**
   - `overflow-y: auto`: 세로 스크롤
   - `min-height: 0`: Flexbox에서 스크롤 활성화

3. **그리드 레이아웃**
   - `grid-template-columns: repeat(auto-fill, minmax(200px, 1fr))`
   - 반응형 그리드 (최소 200px, 자동 채움)

### 3.4 스크롤 문제 해결

**문제**: 전체 페이지가 스크롤되고, 레이아웃이 화면을 벗어남

**해결 과정**:
1. `html, body { height: 100%; }`: 뷰포트 높이 고정
2. `body { display: flex; flex-direction: column; }`: Flexbox 컨테이너
3. `#app { height: 80%; }`: 앱 높이 제한
4. `#grid, #messages { flex: 1; min-height: 0; overflow-y: auto; }`: 내부 스크롤

**결과**: 페이지 스크롤 없음, 내부 패널만 스크롤

---

## 4단계: 검색 결과 그리드

### 4.1 카드 구조

```javascript
results.forEach(r => {
  const card = document.createElement('div');
  card.className = 'card';
  card.tabIndex = 0;
  
  const img = document.createElement('img');
  img.src = r.image_url;
  img.alt = r.image_id;
  
  const caption = document.createElement('div');
  caption.className = 'caption';
  caption.textContent = r.caption;
  
  const similarity = document.createElement('div');
  similarity.className = 'similarity';
  similarity.textContent = `Similarity: ${(r.similarity * 100).toFixed(1)}%`;
  
  card.appendChild(img);
  card.appendChild(caption);
  card.appendChild(similarity);
  gridEl.appendChild(card);
  
  // 클릭 이벤트
  card.addEventListener('click', () => openModal(r));
  card.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' || e.key === ' ') {
      openModal(r);
    }
  });
});
```

**카드 구성**:
- 이미지 썸네일
- 캡션 (일부만 표시, 말줄임표)
- 유사도 점수

**접근성**:
- `tabIndex={0}`: 키보드 포커스 가능
- `Enter`/`Space` 키로 모달 열기

### 4.2 CSS 스타일링

```css
.card {
  background: #1a1f35;
  border-radius: 8px;
  overflow: hidden;
  cursor: pointer;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
}

.card img {
  width: 100%;
  height: 150px;
  object-fit: cover;
}

.caption {
  padding: 12px;
  font-size: 14px;
  color: #b8c5d6;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
}
```

**스타일 특징**:
- 호버 효과: 위로 이동 + 그림자
- 이미지 비율 유지: `object-fit: cover`
- 캡션 말줄임표: 3줄 제한

---

## 5단계: 채팅 인터페이스

### 5.1 메시지 표시

```javascript
function addMsg(text, cls) {
  const div = document.createElement('div');
  div.className = `msg ${cls}`;
  div.textContent = text;
  messagesEl.appendChild(div);
  messagesEl.scrollTop = messagesEl.scrollHeight;
}
```

**메시지 타입**:
- `user`: 사용자 질문
- `bot`: AI 답변

**자동 스크롤**: 새 메시지 추가 시 맨 아래로 스크롤

### 5.2 검색 요청

```javascript
async function send() {
  const q = inputEl.value.trim();
  if (!q) return;
  
  inputEl.value = '';
  addMsg(q, 'user');
  sendBtn.disabled = true;
  gridEl.innerHTML = '';
  addMsg('검색 중...', 'bot');
  
  try {
    const resp = await fetch('/chat', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ message: q, top_k: 24 })
    });
    
    const data = await resp.json();
    const last = messagesEl.lastChild;
    if (last && last.classList.contains('bot')) last.remove();
    addMsg(data.answer || '(응답 없음)', 'bot');
    
    // 검색 결과 그리드에 표시
    displayResults(data.results || []);
  } catch (error) {
    console.error('Error:', error);
    addMsg('오류가 발생했습니다.', 'bot');
  } finally {
    sendBtn.disabled = false;
  }
}
```

**처리 과정**:
1. 사용자 입력 가져오기
2. 사용자 메시지 표시
3. "검색 중..." 표시
4. `/chat` 엔드포인트 호출
5. 답변 표시
6. 검색 결과 그리드에 표시

**에러 처리**: try-catch로 에러 메시지 표시

---

## 6단계: 모달 이미지 뷰어

### 6.1 모달 구조

```html
<div id='imageModal' class='modal hidden'>
  <div class='modal-content'>
    <button id='modalClose' class='modal-close' aria-label='Close'>&times;</button>
    <img id='modalImage' alt='선택된 위성 이미지'>
  </div>
</div>
```

**구성 요소**:
- 배경 오버레이 (클릭 시 닫기)
- 모달 콘텐츠 (이미지 + 닫기 버튼)

### 6.2 모달 열기/닫기

```javascript
function openModal(data) {
  if (!modalEl) return;
  modalImgEl.src = data.image_url;
  modalImgEl.alt = data.image_id || 'Satellite image';
  modalEl.classList.remove('hidden');
  modalEl.classList.add('show');
  document.body.classList.add('modal-open');
}

function closeModal() {
  if (!modalEl) return;
  modalEl.classList.remove('show');
  modalEl.classList.add('hidden');
  modalImgEl.src = '';
  document.body.classList.remove('modal-open');
}
```

**닫기 방법**:
1. 닫기 버튼 클릭
2. 배경 오버레이 클릭
3. `Escape` 키 누르기

### 6.3 CSS 스타일링

```css
.modal {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.9);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 4000;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.3s ease;
}

.modal.show {
  opacity: 1;
  pointer-events: auto;
}

.modal-content {
  position: relative;
  max-width: 90vw;
  max-height: 90vh;
}

.modal-content img {
  max-width: 100%;
  max-height: 90vh;
  object-fit: contain;
}
```

**스타일 특징**:
- 풀스크린 오버레이
- 중앙 정렬된 이미지
- 반응형 (90vw/90vh)
- 페이드 인/아웃 애니메이션

---

## 7단계: 반응형 디자인

### 7.1 미디어 쿼리

```css
@media (max-width: 768px) {
  #layout {
    flex-direction: column;
  }
  
  #results, #chatPane {
    flex: none;
    height: 50%;
  }
}
```

**작은 화면 대응**:
- 2열 → 1열 (세로 배치)
- 각 패널 50% 높이

### 7.2 접근성

**ARIA 속성**:
- `aria-live='polite'`: 메시지 영역 (스크린 리더)
- `aria-label='Close'`: 닫기 버튼

**키보드 네비게이션**:
- `tabIndex={0}`: 포커스 가능
- `Enter`/`Space`: 모달 열기

---

## 🎯 파트 4 핵심 성과

### 기술적 성과

1. **프로덕션 준비 서버**
   - FastAPI 기반 RESTful API
   - 정적 파일 서빙
   - 에러 처리 및 폴백

2. **직관적인 UI**
   - 히어로 섹션으로 시각적 임팩트
   - 2열 레이아웃으로 효율적인 정보 표시
   - 모달 뷰어로 이미지 상세 보기

3. **사용자 경험 최적화**
   - 내부 스크롤로 깔끔한 레이아웃
   - 반응형 디자인
   - 접근성 고려

### UI/UX 개선

- **레이아웃**: 페이지 스크롤 제거, 내부 패널만 스크롤
- **정렬**: 히어로 섹션 완벽한 중앙 정렬
- **애니메이션**: 부드러운 전환 효과

---

## 🔍 면접에서 강조할 포인트

### 1. 레이아웃 설계

**질문**: "UI 레이아웃을 어떻게 설계했나요?"

**답변**:
> "2열 레이아웃으로 검색 결과와 채팅을 동시에 표시하도록 설계했습니다. 페이지 전체는 고정 높이(뷰포트 80%)로 설정하고, 내부 패널(#grid, #messages)만 스크롤되도록 하여 깔끔한 레이아웃을 구현했습니다. Flexbox와 `min-height: 0`을 활용하여 내부 스크롤을 활성화했고, 작은 화면에서는 미디어 쿼리로 1열로 변경하도록 반응형 디자인을 적용했습니다."

### 2. 히어로 섹션 구현

**질문**: "히어로 섹션의 전환 애니메이션을 어떻게 구현했나요?"

**답변**:
> "히어로 섹션은 `position: fixed`로 풀스크린 고정하고, `transform: translateY(-100%)`와 `opacity: 0`으로 위로 슬라이드하며 사라지도록 구현했습니다. CSS transition으로 0.9초 동안 부드럽게 전환하고, JavaScript에서 900ms 후 완전히 숨겨서 성능을 최적화했습니다. 또한 `display: flex`와 `align-items: center`, `justify-content: center`를 사용하여 타이틀과 버튼을 정확히 중앙 정렬했습니다."

### 3. 모달 뷰어

**질문**: "이미지 모달 뷰어의 접근성을 어떻게 고려했나요?"

**답변**:
> "모달 뷰어는 키보드 접근성을 고려하여 구현했습니다. 이미지 카드에 `tabIndex={0}`을 설정하여 키보드 포커스가 가능하도록 했고, `Enter` 또는 `Space` 키로 모달을 열 수 있도록 했습니다. 또한 `Escape` 키로 모달을 닫을 수 있고, 배경 오버레이 클릭으로도 닫을 수 있어 다양한 사용자 인터랙션을 지원합니다. ARIA 속성도 추가하여 스크린 리더 사용자를 고려했습니다."

---

## 📚 관련 파일

- `scripts/api/server.py`: FastAPI 서버
- `scripts/api/web/static/app.js`: 프론트엔드 JavaScript
- `scripts/api/web/static/style.css`: 스타일시트

---

## 🔄 다음 파트로의 연결

파트 4에서 구현한 웹 서비스는 **파트 5 (최적화 및 트러블슈팅)**에서 발생한 문제들을 해결하면서 개선되었습니다. 특히 GPU 메모리 최적화, 캡션 반복 문제, UI 레이아웃 문제 등이 해결되었습니다.

