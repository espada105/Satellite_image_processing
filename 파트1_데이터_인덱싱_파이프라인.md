# 파트 1: 데이터 인덱싱 파이프라인

## 📋 개요

이 파트는 **오프라인 인덱싱 단계**로, 위성 이미지를 나중에 빠르게 검색할 수 있도록 **"텍스트 + 벡터"** 형태로 변환하여 데이터베이스에 저장하는 전체 과정을 다룹니다.

**핵심 목표**: 이미지 → 캡션 → 임베딩 → DB 저장까지의 파이프라인을 구축하여, 이후 텍스트 쿼리로 이미지를 검색할 수 있는 기반을 마련합니다.

---

## 🔄 전체 워크플로우

```
위성 이미지 (GeoTIFF)
    ↓
[1단계] 이미지 캡션 생성 (BLIP)
    ↓
[2단계] 캡션 품질 정제 (후처리)
    ↓
[3단계] 텍스트 임베딩 생성 (sentence-transformers)
    ↓
[4단계] PostgreSQL 벡터 저장
    ↓
검색 가능한 벡터 데이터베이스 완성
```

---

## 1단계: 이미지 캡션 생성 (Image Captioning)

### 1.1 설계 의도

**목적**: 위성 이미지를 보고 자동으로 설명 문장(캡션)을 생성합니다.

**왜 필요한가?**
- 이미지 자체는 벡터 검색이 어렵습니다 (이미지 임베딩은 다른 목적)
- 텍스트로 변환하면 자연어 쿼리로 검색 가능합니다
- 예: "flooded area near river" → 관련 이미지 검색

### 1.2 모델 선택 과정

#### 초기 실험: InstructBLIP, BLIP-2

**시도한 모델들**:
- `Salesforce/instructblip-flan-t5-xl`
- `Salesforce/blip2-opt-2.7b`

**발생한 문제들**:
1. **프롬프트 에코 (Prompt Echoing)**
   - 모델이 프롬프트 문장을 그대로 따라 적는 현상
   - 예: 프롬프트 "A detailed satellite image showing..." → 캡션도 "A detailed satellite image showing..."로 시작
   
2. **반복 문제**
   - "road road road", "country side of the country side" 같은 반복 구문
   - 생성 파라미터 조정으로도 해결되지 않음

3. **배치 처리 이슈**
   - BLIP-2는 배치 처리 시 메모리 부족 또는 에러 발생

#### 최종 선택: BLIP (Salesforce/blip-image-captioning-large)

**선택 이유**:
- 프롬프트 없이도 고품질 캡션 생성
- 배치 처리 안정성
- GPU 메모리 효율성 (8GB RTX 3060 Ti에서도 동작)
- 반복 문제가 상대적으로 적음

**모델 특징**:
- Vision Encoder + Text Decoder 구조
- 이미지만 입력해도 자연스러운 캡션 생성
- 위성 이미지 도메인에서도 양호한 성능

### 1.3 구현 상세

#### 모델 로딩 (`load_caption_model`)

```python
def load_caption_model(model_name: str = "Salesforce/blip-image-captioning-large", 
                       device: str = None, 
                       use_fp16: bool = True):
```

**핵심 구현 포인트**:

1. **자동 디바이스 선택**
   - CUDA 사용 가능 시 GPU, 아니면 CPU
   - `torch.cuda.is_available()` 체크

2. **FP16 최적화**
   - `model.half()`로 모델을 float16으로 변환
   - 메모리 사용량 50% 감소 (4GB → 2GB)
   - RTX 3060 Ti 8GB 환경에서 필수

3. **GPU 메모리 모니터링**
   - 로딩 후 할당/예약 메모리 출력
   - 디버깅 및 최적화에 활용

#### 배치 처리 (`generate_captions_batch_gpu`)

**핵심 설계**:
- 여러 이미지를 한 번에 GPU에 로드하여 병렬 처리
- 순차 처리 대비 **1.8배 속도 향상**

**배치 처리 흐름**:
1. 이미지 로드 및 RGB 변환
2. Processor로 이미지 전처리 (정규화, 리사이즈)
3. 배치로 묶어서 GPU에 전송
4. `model.generate()`로 배치 생성
5. `processor.batch_decode()`로 텍스트 변환

**생성 파라미터 최적화**:

```python
generate_kwargs = {
    "max_length": 120,           # 최대 길이 (기본 50 → 120으로 증가)
    "min_length": 20,             # 최소 길이
    "num_beams": 5,               # 빔 서치 개수
    "do_sample": True,            # 샘플링 활성화 (다양성 증가)
    "temperature": 0.8,           # 온도 (높을수록 다양)
    "repetition_penalty": 1.3,    # 반복 방지 (1.0보다 크면 반복 감소)
    "length_penalty": 1.2,        # 길이 페널티 (1.0보다 크면 긴 시퀀스 선호)
}
```

**각 파라미터의 역할**:
- `repetition_penalty=1.3`: 이미 생성한 토큰의 확률을 낮춰 반복 감소
- `temperature=0.8`: 샘플링 다양성 제어 (너무 높으면 무작위, 너무 낮으면 그리디)
- `length_penalty=1.2`: 짧은 캡션보다 긴 캡션 선호 (정보량 증가)

#### 동적 배치 크기 조정

**문제**: 고정 배치 크기(16) 사용 시 OOM 에러

**해결**: GPU 메모리 여유에 따라 자동 조정
- `torch.cuda.mem_get_info()`로 사용 가능 메모리 확인
- 2/4/8/16 중 자동 선택
- 메모리 부족 시 CPU 폴백

### 1.4 결과 및 통계

**생성된 캡션 예시**:
- "A satellite image showing a rural area with a road running through the middle, surrounded by trees and green fields on both sides."
- "Google Earth view of a flooded area with houses partially submerged in water, surrounded by trees and vegetation."

**품질 지표**:
- 평균 캡션 길이: 50+ 단어
- 반복 비율: 초기 30% → 최적화 후 15% (후처리 전)

---

## 2단계: 캡션 품질 정제 (Text Cleaning)

### 2.1 문제 인식

**발견된 문제**:
- "road road road" (연속 단어 반복)
- "country side of the country side" (구문 반복)
- "flooded area flooded area" (2-3단어 구문 반복)

**영향**:
- 검색 정확도 저하 (중복 패턴이 벡터 공간을 왜곡)
- 사용자 경험 저하 (읽기 어려운 캡션)

### 2.2 반복 감지 알고리즘 (`detect_repetition`)

**2단계 감지**:

1. **연속 단어 반복**
   - 같은 단어가 3번 이상 연속으로 나타나는지 확인
   - 예: "road road road" → 감지

2. **구문 반복**
   - 2-5개 단어 구문이 연속으로 반복되는지 확인
   - 예: "country side of the country side" → 감지

**알고리즘**:
```python
# 연속 단어 반복 체크
for i in range(len(words) - 2):
    if words[i] == words[i+1] == words[i+2]:
        return True

# 구문 반복 체크 (슬라이딩 윈도우)
for length in range(2, min(5, len(words) // 2 + 1)):
    for i in range(len(words) - length * 2 + 1):
        segment1 = words[i:i+length]
        segment2 = words[i+length:i+length*2]
        if segment1 == segment2:
            return True
```

### 2.3 반복 제거 알고리즘 (`fix_repetition`)

**3단계 처리**:

1. **연속 단어 제거**
   - 2개 이상 연속된 동일 단어 → 1개만 유지
   - 예: "road road road" → "road"

2. **구문 반복 제거 (반복적 처리)**
   - 2-6개 단어 구문이 반복되면 첫 번째만 유지
   - 여러 번 반복될 수 있으므로 최대 10회 반복 처리
   - 예: "country side of the country side" → "country side of the"

3. **최종 정제**
   - 불필요한 공백 제거
   - 긴 캡션(100단어 이상)에서 심한 반복이 있으면 처음 100단어만 유지

**핵심 로직**:
```python
# 구문 반복 제거 (다중 반복 처리)
max_iterations = 10
while iteration < max_iterations:
    # 슬라이딩 윈도우로 반복 구문 찾기
    for length in range(2, min(7, (len(fixed_words) - i) // 2 + 1)):
        segment1 = [w.lower() for w in fixed_words[i:i+length]]
        segment2 = [w.lower() for w in fixed_words[i+length:i+length*2]]
        if segment1 == segment2:
            # 첫 번째만 유지, 나머지 제거
            result_words.extend(fixed_words[i:i+length])
            # 추가 반복도 확인하여 모두 제거
            j = i + length * 2
            while j + length <= len(fixed_words):
                segment_next = [w.lower() for w in fixed_words[j:j+length]]
                if segment_next == segment1:
                    j += length  # 반복 구문 스킵
                else:
                    break
            i = j
```

### 2.4 결과

**처리 통계**:
- 반복 감지율: 약 30%
- 수정률: 약 25%
- 반복 비율: 30% → 5% 미만 (후처리 후)

**개선 효과**:
- 벡터 공간에서 중복 패턴 제거 → 검색 정확도 향상
- 가독성 향상 → 사용자 경험 개선

---

## 3단계: 텍스트 임베딩 생성 (Sentence Embedding)

### 3.1 설계 의도

**목적**: 정제된 캡션 텍스트를 **고정 길이 벡터**로 변환합니다.

**왜 필요한가?**
- 텍스트를 벡터로 변환해야 **코사인 유사도**로 검색 가능
- "flooded area" 쿼리와 "flooded region" 캡션이 벡터 공간에서 가까워야 함

### 3.2 모델 선택: all-MiniLM-L6-v2

**선택 이유**:
- **경량 모델**: 384차원, 빠른 추론 속도
- **범용성**: 다양한 도메인에서 양호한 성능
- **sentence-transformers 라이브러리**: 간편한 사용
- **영어 최적화**: 위성 이미지 캡션이 영어이므로 적합

**모델 특징**:
- 입력: 텍스트 문장
- 출력: 384차원 벡터 (numpy array)
- 의미적으로 유사한 문장은 벡터 공간에서 가까움

### 3.3 구현 상세

#### 임베딩 생성 (`generate_embeddings`)

**핵심 구현**:
```python
model = SentenceTransformer(model_name, device=device)
embeddings = model.encode(
    caption_texts,
    batch_size=batch_size,
    show_progress_bar=True,
    convert_to_numpy=True
)
```

**배치 처리**:
- 기본 배치 크기: 32
- GPU 사용 시 64까지 증가 가능
- CPU 사용 시 32 유지 (메모리 제약)

**결과 형태**:
- Shape: `(n_samples, 384)`
- 예: 1000개 캡션 → (1000, 384) 배열

#### 저장 형식

**NumPy 배열 (.npy)**:
- 효율적인 바이너리 저장
- 빠른 로딩 속도

**메타데이터 (.json)**:
- 임베딩 차원, 샘플 수, 모델 정보 저장
- 나중에 검증 및 디버깅에 활용

### 3.4 벡터 차원 선택 이유

**384차원 선택**:
- **충분한 표현력**: 의미적 유사성을 잘 포착
- **효율성**: 768차원 대비 메모리/속도 절반
- **검색 성능**: 코사인 유사도 계산이 빠름

**대안 고려**:
- 768차원 (all-mpnet-base-v2): 더 정확하지만 느림
- 128차원: 빠르지만 표현력 부족
- **결론**: 384차원이 속도-정확도 균형 최적

---

## 4단계: PostgreSQL 벡터 저장

### 4.1 데이터베이스 스키마 설계

**테이블 구조**:
```sql
CREATE TABLE satellite_images (
    id SERIAL PRIMARY KEY,
    image_id VARCHAR(255) UNIQUE NOT NULL,
    image_path TEXT NOT NULL,
    caption TEXT,
    embedding_array REAL[],  -- 384차원 벡터 배열
    metadata JSONB,
    location_name VARCHAR(255),
    latitude REAL,
    longitude REAL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**핵심 설계 결정**:

1. **REAL[] 배열 사용 (pgvector 미사용)**
   - **이유**: pgvector 설치 복잡도, 호환성 문제
   - **대안**: NumPy로 코사인 유사도 직접 계산
   - **장점**: 단순성, 유연성
   - **단점**: 대규모 데이터에서 성능 저하 가능 (하지만 현재 규모에서는 충분)

2. **ON CONFLICT 처리**
   - `image_id`가 UNIQUE이므로 중복 방지
   - `ON CONFLICT DO UPDATE`: 기존 레코드 업데이트 (캡션/임베딩 갱신)

3. **메타데이터 JSONB**
   - 이벤트 타입 (PRE/POST-event)
   - 데이터셋 정보
   - 해상도 정보
   - 하이브리드 검색에 활용

### 4.2 삽입 프로세스 (`insert_to_database`)

**배치 삽입**:
- 기본 배치 크기: 100
- 트랜잭션 단위로 커밋 (안정성)

**에러 처리**:
- 개별 레코드 삽입 실패 시 스킵하고 계속 진행
- 처음 5개 오류만 출력 (로그 과다 방지)

**중복 처리**:
- `skip_existing=True`: 기존 레코드 건너뛰기
- `skip_existing=False`: 기존 레코드 업데이트 (캡션/임베딩 갱신)

### 4.3 검증 (`verify_database`)

**확인 항목**:
- 총 레코드 수
- 임베딩 포함 레코드 수
- 메타데이터 포함 레코드 수
- 샘플 데이터 조회

---

## 🎯 파트 1 핵심 성과

### 기술적 성과

1. **멀티모달 파이프라인 구축**
   - 이미지 → 텍스트 → 벡터 변환 완성
   - 전체 자동화 (스크립트 실행만으로 처리)

2. **GPU 최적화**
   - FP16으로 메모리 50% 감소
   - 동적 배치 크기로 OOM 에러 제로
   - 처리 속도 1.8배 향상

3. **품질 개선**
   - 캡션 평균 길이 50+ 단어
   - 반복 비율 30% → 5% 미만
   - 검색 정확도 향상

### 데이터 통계

- 처리된 이미지: SpaceNet SN8 데이터셋 전체
- 생성된 캡션: 이미지 수와 동일
- 벡터 차원: 384차원
- 저장 형식: PostgreSQL REAL[] 배열

---

## 🔍 면접에서 강조할 포인트

### 1. 모델 선택 과정

**질문**: "왜 BLIP을 선택했나요?"

**답변**:
> "초기에 InstructBLIP과 BLIP-2를 실험했지만, 프롬프트 에코 문제와 반복 문제가 발생했습니다. 특히 InstructBLIP은 프롬프트를 그대로 따라 적는 현상이 있었고, BLIP-2는 배치 처리 시 메모리 이슈가 있었습니다. 최종적으로 BLIP (blip-image-captioning-large)를 선택했는데, 프롬프트 없이도 고품질 캡션을 생성하고, GPU 메모리 효율성이 좋아 8GB GPU에서도 안정적으로 동작했습니다."

### 2. 반복 문제 해결

**질문**: "캡션 반복 문제를 어떻게 해결했나요?"

**답변**:
> "생성 파라미터 최적화와 후처리 스크립트 두 가지 접근을 사용했습니다. 먼저 `repetition_penalty=1.3`, `temperature=0.8` 등으로 생성 시 반복을 줄였고, 그럼에도 불구하고 발생하는 반복은 후처리 스크립트로 제거했습니다. 후처리 알고리즘은 연속 단어 반복과 구문 반복(2-6단어)을 감지하여 제거하는 방식으로, 반복 비율을 30%에서 5% 미만으로 줄였습니다."

### 3. 벡터 저장 방식

**질문**: "왜 pgvector를 사용하지 않았나요?"

**답변**:
> "pgvector를 사용하지 않고 PostgreSQL의 REAL[] 배열을 사용한 이유는, 설치 복잡도와 호환성 문제를 피하기 위해서입니다. 대신 NumPy로 코사인 유사도를 직접 계산하는 방식을 선택했는데, 현재 데이터 규모에서는 충분한 성능을 보였고, 코드가 더 단순하고 유지보수가 쉬웠습니다. 향후 대규모 확장이 필요하면 pgvector나 전문 벡터 DB로 마이그레이션할 계획입니다."

---

## 📚 관련 파일

- `scripts/data_collection/generate_captions.py`: 캡션 생성 스크립트
- `scripts/data_collection/fix_caption_repetition.py`: 반복 제거 후처리
- `scripts/data_collection/generate_embeddings.py`: 임베딩 생성 스크립트
- `scripts/database/insert_to_db.py`: DB 삽입 스크립트

---

## 🔄 다음 파트로의 연결

파트 1에서 구축한 벡터 데이터베이스는 **파트 2 (벡터 검색 시스템)**에서 활용됩니다. 사용자의 텍스트 쿼리를 임베딩으로 변환하고, 저장된 벡터와 코사인 유사도를 계산하여 관련 이미지를 검색합니다.

